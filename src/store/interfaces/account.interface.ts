export interface IAccount {
    name: string | null,
    grade: number | null,
    school: string | null,
    studentId: string | null,
    stateId: string | null,
    lunchBalance: string | null,
    locker: string | null,
}